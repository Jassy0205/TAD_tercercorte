if real != None: 
                    vector = [salida]
                    vector.extend(real)
                    vector.append(vecino)
                else: 
                    vector = [salida, u, vecino]